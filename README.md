# push-stream-chat-docusaurus-action

## Example usage
```yaml
      - name: Push
        uses: GetStream/push-stream-chat-docusaurus-action@main
        env:
          DOCUSAURUS_GH_TOKEN: ${{ secrets.DOCUSAURUS_GH_TOKEN }}
```